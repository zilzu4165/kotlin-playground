package lec01

class Lec04Main {
}

/**
 *  in / !in
 *  - 컬렉션이나 범위에 포함되어 있다, 포함되어 있지 않다.
 *  a..b
 *  - a부터 b까지의 범위 객체를 생성한다.
 *  a[i]
 *  - a에서 특정 index i 로 값을 가져온다.
 *  a[i] = b
 *  - a의 특정 index i에 b를 넣는다
 */

fun main() {

}